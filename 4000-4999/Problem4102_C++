#include <iostream>
#include <string>

using namespace std;

int n;
string a, b;

int main()
{
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> a >> b;
		a[0] += 32;
		b[0] += 32;
		cout << b << "." << a << "@retratek.com" << endl;
	}
}
