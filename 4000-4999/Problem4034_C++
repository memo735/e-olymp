#include <iostream>
#include <algorithm>

using namespace std;

int n, d[1000];

int main()
{
	cin >> n;
	for (int i = 0; i < n; i++) cin >> d[i];
	sort(d, d + n);
	cout << d[0];
	for (int i = 1; i < n; i++) cout << " " << d[i];
	cout << endl;
}
