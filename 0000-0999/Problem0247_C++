#include <iostream>
#include <fstream>

using namespace std;
fstream in("input.txt", ios::in), out("output.txt", ios::out);

long long n, ans = 0, tmp;

int main()
{
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> tmp;
		ans += (bool)(tmp & 2);
	}
	cout << ans << endl;
}
