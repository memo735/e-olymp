using System;

namespace Problem494
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            string s = Console.ReadLine();
            int len = s.Length;
            int say = 0;
            for (int i = 0; i < len; i++)
            {
                if (s[i] == 'A' || s[i] == 'E' || s[i] == 'I' || s[i] == 'O' || s[i] == 'U' || s[i] == 'Y') say++;
            }
            Console.WriteLine(say);
        }
    }
}
