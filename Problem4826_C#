using System;

namespace Problem4826
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            string setir = Console.ReadLine();
            var massiv = setir.Split(' ');
            int a = int.Parse(massiv[0]);
            int b = int.Parse(massiv[1]);
            for (int i = a; i <= b; i++)
            {
                Console.Write(i);
                if (i < b) Console.Write(" ");
            }
            Console.WriteLine();
        }
    }
}
