#include <iostream>

using namespace std;

int a, b, c;

int main()
{
	cin >> a >> b >> c;
	if ((a % 2 || b % 2 || c % 2) && ((a + 1) % 2 || (b + 1) % 2 || (c + 1) % 2)) cout << "YES" << endl;
	else cout << "NO" << endl;
}
