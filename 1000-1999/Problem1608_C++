#include <iostream>
#include <string>

using namespace std;

string str;

int main()
{
	cin >> str;
	for (int i = 0; i < str.length() / 2; i++)
		if (str[i] != str[str.length() - i - 1])
		{
			cout << "No" << endl;
			return 0;
		}
	cout << "Yes" << endl;
}
