#include <iostream>
#include <algorithm>
#include <stack>

using namespace std;

int n, tmp;
stack <int> st;

int main()
{
	cin >> n;
	st.push(0);
	for (int i = 0; i < n; i++)
	{
		cin >> tmp;
		if (tmp) st.push(max(st.top(), tmp));
		else
		{
			st.pop();
			cout << st.top() << endl;
		}
	}
}
