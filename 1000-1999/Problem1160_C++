#include <iostream>

using namespace std;

int main()
{
	int n, MAX = 0, ind = -2, a, b; cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> a >> b;
		if (b && a > MAX)
		{
			MAX = a;
			ind = i;
		}
	}
	cout << ind + 1 << endl;
}
