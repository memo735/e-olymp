#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
	int n, MAX, MIN; cin >> n;
	if (n < 2)
	{
		cout << "Ooops!\n" << endl;
		return 0;
	}
	int t; cin >> t;
	MAX = MIN = t;
	for (int i = 0; i < n - 1; i++)
	{
		cin >> t;
		MAX = max(MAX, t);
		MIN = min(MIN, t);
	}
	cout << MIN << " " << MAX << endl;
}
