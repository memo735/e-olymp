using System;
using System.Numerics;

namespace Problem5067
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            BigInteger a = BigInteger.Parse(Console.ReadLine());
            int b = int.Parse(Console.ReadLine());
            Console.WriteLine(a/b);
            Console.WriteLine(a%b);
        }
    }
}
