#include <iostream>

using namespace std;

long long test, counT, ans, tmpa, tmpb;

int main()
{
	cin >> test;
	for (int i = 0; i < test; i++)
	{
		cin >> ans >> counT;
		for (int j = 0; j < counT; j++)
		{
			cin >> tmpa >> tmpb;
			ans += tmpa * tmpb;
		}
		cout << ans << endl;
	}
}
