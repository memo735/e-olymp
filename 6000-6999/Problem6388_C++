#include <iostream>

using namespace std;

int main()
{
	int p; cin >> p;
	for (int i = 0; i < p; i++) {
		int n;
		float d, a, b, f, result;
		scanf("%d %f %f %f %f", &n, &d, &a, &b, &f);
		result = f*d / (a + b);
		printf("%d %1.2f\n", n, result);
	}
	return 0;
}
