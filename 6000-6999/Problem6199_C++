#include <iostream>

using namespace std;

int main() {
	int n, test; cin >> test;
	for (int i = 0; i < test; ++i)
	{
		cin >> n;
		if (n % 2 == 0) cout << n << " is even" << endl;
		else cout << n << " is odd" << endl;
	}
	return 0;
}
