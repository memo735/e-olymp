#include <iostream>
#include <cmath>

double fun(double x){
	return 3 * sin(x);
}

int main(){
	double a, b, h;
	std::cin >> a >> b >> h;
	while (a <= b){
		printf("%.3lf %.3lf\n", a, fun(a));
		a += h;
	}
	return 0;
}
