#include <iostream>

using namespace std;

long long n, tmp, sum = 0;

int main()
{
	cin >> n;
	sum = n*(n + 1) / 2;
	for (long i = 1; i < n; i++)
	{
		cin >> tmp;
		sum -= tmp;
	}
	cout << sum << endl;
}
