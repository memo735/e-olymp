using System;
using System.Numerics;

namespace Problem2163
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            BigInteger n = BigInteger.Parse(Console.ReadLine());
            Console.WriteLine(BigInteger.ModPow(n, 1, 3) == 0 ? "YES" : "NO");
        }
    }
}
