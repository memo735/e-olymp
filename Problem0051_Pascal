var s, n, k, t:longint;
begin
  readln (s);
  if s=0 then begin writeln (0); halt; end;
  k:=0; t:=1;
  while t<s do
    begin
      k:=k+1; t:=t+k+1;
    end;
  if s<=t then inc(k);
  writeln (k);
end.
